/*func divideIfWhole(value: Int, divisor: Int) -> Int? {
    var count = 0;
    
    if (value % divisor != 0) {
        return count
    }
    count = value / divisor
    
    return count
}
print("Enter the value:")
let value = readLine() // Remember, this is an optional string
print("Enter the divisor:")
let divisor = readLine() // Remember, this is an optional string

let convertedValue: Int? = Int (value!)
let convertedDivisor: Int? = Int (divisor!)

let count = divideIfWhole(value: convertedValue!, divisor: convertedDivisor!)!

if (count != 0) {
    if (count == 1) {
        print("Yep, it divides \(count) time")
    } else {
        print("Yep, it divides \(count) times")
    }
} else {
    print("Not divisible :((")
}
*/
